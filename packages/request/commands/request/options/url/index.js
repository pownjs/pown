module.exports = {
    'url-prefix': {
        alias: [],
        type: 'string',
        describe: 'Add prefix to each url'
    },

    'url-suffix': {
        alias: [],
        type: 'string',
        describe: 'Add suffix to each url'
    }
}
